[gd_scene load_steps=5 format=3 uid="uid://d2vk1qmrvldfa"]

[ext_resource type="Texture2D" uid="uid://06133qxs2wld" path="res://icon.svg" id="1_dr5h2"]

[sub_resource type="GDScript" id="GDScript_fahyk"]
script/source = "extends CharacterBody2D


const SPEED = 300.0
const JUMP_VELOCITY = -400.0

# Get the gravity from the project settings to be synced with RigidBody nodes.
var gravity = ProjectSettings.get_setting(\"physics/2d/default_gravity\")


func _physics_process(delta):
	# Add the gravity.
	if not is_on_floor():
		velocity.y += gravity * delta

	# Handle Jump.
	if Input.is_action_just_pressed(\"ui_accept\") and is_on_floor():
		velocity.y = JUMP_VELOCITY

	# Get the input direction and handle the movement/deceleration.
	# As good practice, you should replace UI actions with custom gameplay actions.
	var direction = Input.get_axis(\"ui_left\", \"ui_right\")
	if direction:
		velocity.x = direction * SPEED

	move_and_slide()
"

[sub_resource type="SpriteFrames" id="SpriteFrames_14twl"]
animations = [{
"frames": [{
"duration": 1.0,
"texture": ExtResource("1_dr5h2")
}],
"loop": true,
"name": &"default",
"speed": 5.0
}]

[sub_resource type="CapsuleShape2D" id="CapsuleShape2D_bdymi"]
radius = 64.0
height = 128.0

[node name="Player" type="CharacterBody2D"]
script = SubResource("GDScript_fahyk")

[node name="AnimatedSprite2D" type="AnimatedSprite2D" parent="."]
sprite_frames = SubResource("SpriteFrames_14twl")

[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
shape = SubResource("CapsuleShape2D_bdymi")
